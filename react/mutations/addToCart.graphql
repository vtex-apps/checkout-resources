# import '../fragments/orderForm.graphql'

mutation addToCart($items: [ItemInput], $marketingData: MarketingDataInput) {
  addToCart(items: $items, marketingData: $marketingData) {
    ...OrderFormFragment
  }
}
